---
id: vat-k7p
---

Cargo binary crate named `vat`. Pick CLI argument parser (clap derive). Set up `cargo fmt` / `cargo clippy` config. Add a minimal `main.rs` that wires up subcommands as stubs returning "not yet implemented" so the surface area is testable end-to-end early.
