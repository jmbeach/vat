[package]
name = "vat"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1"
clap = { version = "4", features = ["derive"] }
rand = "0.8"
serde_yaml = "0.9"
thiserror = "1"
toml = { version = "0.8", features = ["preserve_order"] }

[dev-dependencies]
tempfile = "3"

[lints.clippy]
pedantic = "warn"

